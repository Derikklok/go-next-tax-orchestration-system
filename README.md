```bash
git branch            # list local branches
git branch -a         # include remote branches
git checkout main     # switch back to main
git branch -d feature/add-handler   # delete after merging in local
git push origin --delete feat/add-handler # delet remote branch
```
